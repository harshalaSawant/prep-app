export interface GenericResIF {
  success: boolean;
  injectable: boolean;
  message: string;
}

export interface ComponentDropDownIF {
  name: string;
  route: string; 
}

export interface StudentDropDownIF {
  name: string;
  id: number; 
}